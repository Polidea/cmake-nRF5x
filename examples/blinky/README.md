# NRF52 Blinky Example

### Configuration

`nrf-toolchain.cmake` for your local cross-compiler toolchain configuration.

`arm-gcc-toolchain.cmake` for your local nrf tools path configuration.

`src/CMakeLists.txt` for your project details.
